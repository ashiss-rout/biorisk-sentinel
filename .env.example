# Use a long random value in production. Do not commit your real .env file.
SECRET_KEY=replace-with-a-long-random-secret
# Local default: sqlite:///instance/biorisk.db
# For a cloud database, set DATABASE_URL to its PostgreSQL connection URL.
DATABASE_URL=
